# Use HTML Fallback Extraction

## Decision

Because there is no confirmed stable public detail API, HTML fallback is added after `window.__PRERENDERED_STATE__` and JSON-LD to improve resilience.

## Consequences

The project remains small while still showing clean engineering decisions.
